{
  "version": 2,
  "framework": "nextjs",
  "rootDirectory": "client",
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/api/graphql",
      "destination": "/server/api/graphql"
    }
  ]
}
